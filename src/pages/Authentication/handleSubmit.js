export const handleLogin = (signed, setSigned) => () => {
  // TODO: Integrar chamada a api por login
  setSigned(true);
};

export const handleRegister = (signed, setSigned) => () => {
  // TODO: Integrar chamada a api por registro
};
