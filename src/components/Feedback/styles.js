import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    position: 'fixed',
    bottom: '24px',
    left: '24px',
  },
  buttonSend: {
    color: '#28a745',
  },
});

export default useStyles;
