import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    textAlign: 'center',
    display: 'block',
  },
  avatar: {
    height: '120px',
    width: '120px',
    margin: '12px auto',
  },
  icon: {
    fontSize: '60px',
  },
});

export default useStyles;
